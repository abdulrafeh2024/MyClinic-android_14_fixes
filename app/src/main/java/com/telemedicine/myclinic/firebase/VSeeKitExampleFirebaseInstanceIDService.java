package com.telemedicine.myclinic.firebase;

/*import com.google.firebase.iid.FirebaseInstanceId;
import com.google.firebase.iid.FirebaseInstanceIdService;*/
import com.google.firebase.messaging.FirebaseMessaging;
import com.google.firebase.messaging.FirebaseMessagingService;
//import com.vsee.kit.VSeeNotificationCenter;
//import com.vsee.notification.NotificationCenter;

/**
 * Created by ruozhen on 4/17/17.
 */

/*
public class VSeeKitExampleFirebaseInstanceIDService extends FirebaseMessaging {
    public static final String TAG = "FirebaseIDService";

    */
/**
     * Called if InstanceID token is updated. This may occur if the security of
     * the previous token had been compromised. Note that this is called when the InstanceID token
     * is initially generated so this is where you would retrieve the token.
     *//*

    @Override
    public void onTokenRefresh() {
        //NotificationCenter.setPushNotificationRegistered(false);
        String refreshedToken = FirebaseMessaging.getInstance().getToken();
        //VSeeNotificationCenter.instance().setFirebaseInstanceIDToken(refreshedToken);
    }

}
*/
